# React + Vite

This template provides a minimal setup to get React working in Vite with HMR and some ESLint rules.

# Real time chat useing webSockets(using socket.io)

- Build the UI for a chat window on /chat/:targetUserId
- setUp the Socket.io in backend
- npm i socket.io
- 
- # to add the routing in the application.
- --to use react-router package.--npm install "react-router-dom"
- --roting , can be done in the root level.
- 
- -First create the <BrowserRouter><BrowsweRouter/>
- inside BrowserRouter, we give me the base router.<BrowserRouter basename="/">, basically all the routes inside it , it will work relative to this path.
- <Routes> components is basically wraper for different routes.